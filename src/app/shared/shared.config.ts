import { ICHTHYOMETER_LOCAL_SETTINGS_OPTIONS } from '@app/shared/ichthyometer/ichthyometer.config';

export const SHARED_LOCAL_SETTINGS_OPTIONS = Object.freeze({
  // Ignore, because added by app.component.ts
  //...ICHTHYOMETER_LOCAL_SETTINGS_OPTIONS

});
