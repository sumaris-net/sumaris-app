import { PredefinedColors } from '@ionic/core';
import { ThemePalette } from '@angular/material/core';

export declare type AppColors = PredefinedColors | ThemePalette | 'secondary100' | 'warning900' | 'danger100' | 'danger900';
